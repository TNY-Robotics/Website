# Wiring Everything

All the PCBs, components, and motors are now securely mounted to the torso, so it's time to wire everything together!

Only one rule here : **Don't mix up the connections**, otherwise your robot will walk funny *(or not walk at all)* !

---

## Wire the Motors

We'll first connect the motors to the Paw Boards.

Remember one thing : These motors are for the `Hip-Roll` joints, so you should plug them in the connectors with the `HR` label next to them !

### The Front-Left motor
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/front-left-motor-wire.webm" />

#text
Connect the **Front-Left motor** to the **first connector on the left** of the **Front Paw Board**.

Make sure you are plugging it in the right way, with the **black wire towards the center** of the board.
::

### The Back-Left motor
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/back-left-motor-wire.webm" />

#text
Connect the **Back-Left motor** to the **first connector on the left** of the **Back Paw Board**.

Make sure you are plugging it in the right way, with the **black wire towards the center** of the board.
::

### The Back-Right motor
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/back-right-motor-wire.webm" />

#text
Connect the **Back-Right motor** to the **first connector on the right** of the **Back Paw Board**.

Make sure you are plugging it in the right way, with the **black wire towards the center** of the board.
::

### The Front-Right motor
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/front-right-motor-wire.webm" />

#text
Connect the **Front-Right motor** to the **first connector on the right** of the **Front Paw Board**.

Make sure you are plugging it in the right way, with the **black wire towards the center** of the board.
::

---

## Wire the Motor Driver

Now let's wire the Motor Driver to the Paw Boards.

For this step, you'll need :

| Connector type | Wire length | Quantity |
| -------------- | ----------- | :------: |
| JST-PH 3-Pin   | 10cm        |    4     |

### The Front-Left wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-front-left.webm" />

#text
Connect one end of a `JST-PH 3-Pin` wire to the **Front-Left connector** of the Motor Driver (with the **1** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Left Motor Driver Connector** of the **Front Paw Board** (with the :logo{name="driver"} Motor Driver icon next to it).

::

### The Back-Left wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-back-left.webm" />

#text
Connect one end of a `JST-PH 3-Pin` wire to the **Back-Left connector** of the Motor Driver (with the **2** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Left Motor Driver Connector** of the **Front Paw Board** (with the :logo{name="driver"} Motor Driver icon next to it).
::

### The Back-Right wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-back-right.webm" />

#text
Connect one end of a `JST-PH 3-Pin` wire to the **Back-Right connector** of the Motor Driver (with the **3** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Right Motor Driver Connector** of the **Back Paw Board** (with the :logo{name="driver"} Motor Driver icon next to it).
::

### The Front-Right wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-front-right.webm" />

#text
Connect one end of a `JST-PH 3-Pin` wire to the **Front-Right connector** of the Motor Driver (with the **4** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Right Motor Driver Connector** of the **Front Paw Board** (with the :logo{name="driver"} Motor Driver icon next to it).
::

---

## Wire the Analog Reader

Moving on to the Analog Reader, we'll connect it to the Paw Boards as well.

For this step, you'll need :

| Connector type | Wire length | Quantity |
| -------------- | ----------- | :------: |
| JST-PH 4-Pin   | 10cm        |    4     |


### The Front-Left wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/analog-reader-front-left.webm" />

#text
Connect one end of a `JST-PH 4-Pin` wire to the **Front-Left connector** of the Analog Reader (with the **1** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Left Analog Reader Connector** of the **Front Paw Board** (with the :logo{name="reader"} Analog Reader icon next to it).
::

### The Back-Left wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/analog-reader-back-left.webm" />

#text
Connect one end of a `JST-PH 4-Pin` wire to the **Back-Left connector** of the Analog Reader (with the **2** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Left Analog Reader Connector** of the **Back Paw Board** (with the :logo{name="reader"} Analog Reader icon next to it).
::

### The Back-Right wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/analog-reader-back-right.webm" />

#text
Connect one end of a `JST-PH 4-Pin` wire to the **Back-Right connector** of the Analog Reader (with the **3** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Right Analog Reader Connector** of the **Back Paw Board** (with the :logo{name="reader"} Analog Reader icon next to it).
::

### The Front-Right wire
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/analog-reader-front-right.webm" />

#text
Connect one end of a `JST-PH 4-Pin` wire to the **Front-Right connector** of the Analog Reader (with the **4** :logo{name="paw"} Paw Board icon next to it).

Connect the other end of the wire to the **Right Analog Reader Connector** of the **Front Paw Board** (with the :logo{name="reader"} Analog Reader icon next to it).
::

---

## Wire the Paw Boards

We'll now connect the two Paw Boards to the Buck Converter.

This will allow them to get power for the motors.

### Unscrew the Buck Converter connector
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/dc-dc-unscrew.webm" />

#text
First, **unscrew the two screws** of the ***OUT* connector** on the Buck Converter.

**Don't loose the screws !** You'll need them !
::

### Connect the Ground
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/dc-dc-ground.webm" />

#text
Place the **Ground Wires of the two Paw Boards** on the **Gound side of the Buck Converter's connector** (with the *OUT-* text label next to it).

**Screw the two wires** in place, making sure they are secure and have a **good contact with the metal** of the connector.
::

### Connect the Power
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/dc-dc-power.webm" />

#text
As for the Ground Wires, place the **Power Wires of the two Paw Boards** on the **Power side of the Buck Converter's connector** (with the *OUT+* text label next to it).

**Screw the two wires** in place, making sure they are secure and have a **good contact with the metal** of the connector.
::

---

## Additional wires

We'll now add a couple more wires to the torso : The wires towards the Head of the robot !

For this step, you'll need :

| Connector type | Wire length | Quantity |
| -------------- | ----------- | :------: |
| JST-PH 2-Pin   | 10cm        |    1     |
| JST-PH 5-Pin   | 10cm        |    1     |
| JST-PH 7-Pin   | 10cm        |    1     |

### Motor Driver main connector
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-main.webm" />

#text
Use the `JST-PH 5-Pin` wire, and connect one end to the Motor Driver's :logo{name="brain"} Brain connector.

Move the other side of the connector towards the front of the robot (where the Head will be).
::

### Motor Driver ear connector
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/motor-driver-ear.webm" />

#text
Use the `JST-PH 2-Pin` wire, and connect one end to the Motor Driver's :logo{name="ears"} Ears connector.

Move the other side of the connector towards the front of the robot.
::

### Analog Reader main connector
::step
#animation
<Animation src="/docs/images/assembly/torso/wiring/analog-reader-main.webm" />

#text
Use the `JST-PH 7-Pin` wire, and connect one end to the Analog Reader's :logo{name="brain"} Brain connector.

Move the other side of the connector towards the front of the robot.
::

---

## Verification

Nice ! Everything should be wired up correctly now.

Make sure all the wires are plugged in the right connectors, and that they are all secured in place !

Here's what the final result should look like at this stage :

::center
<img src="/docs/images/assembly/torso/verification.jpg" class="w-2/3" alt="<image>" />
::

---

We can now get started on the head, let's see how to do that !

:nav-buttons{left="Adding the motors" left-link="./motors" left-icon="i-lucide-chevron-left" right="The Head" right-link="../head" right-icon="i-lucide-chevron-right"}
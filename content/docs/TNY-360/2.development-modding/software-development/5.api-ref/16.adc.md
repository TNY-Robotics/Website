# ADC

`ADC` module of the TNY-360 API (code `0x0F`).

This module contains actions related to the ADC communication with external devices, like reading feedback voltage levels.

---

## List of actions

Here's a list of all the actions available in the `adc` module:

:api-actions{module="adc"}

## List of types

Here's a list of all the types used in the `adc` module:

:api-types{module="adc"}

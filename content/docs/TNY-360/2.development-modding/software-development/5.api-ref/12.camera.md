# Camera

`Camera` module of the TNY-360 API (code `0x0C`).

This module contains actions related to camera control, such as capturing images, settings resolution and frame rate.

---

## List of actions

Here's a list of all the actions available in the `camera` module:

:api-actions{module="camera"}

## List of types

Here's a list of all the types used in the `camera` module:

:api-types{module="camera"}
